spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: consul-miya
        heartbeat:
          enabled: true
  application:
    name: consul-miya
server:
  port: 8507